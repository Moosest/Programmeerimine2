<Window x:Class="KooliProjekt.WpfApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Clients" Height="520" Width="980" MinHeight="520" MinWidth="980">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="16" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="clientsGrid"
                  Grid.Column="0"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  ItemsSource="{Binding Data}"
                  SelectedItem="{Binding SelectedItem}"
                  Margin="0,0,0,0" />

        <Border Grid.Column="2" BorderBrush="#DDDDDD" BorderThickness="1" Padding="14" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="8" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Id" />
                <TextBox Grid.Row="2" IsReadOnly="True" Text="{Binding SelectedItem.Id}" />

                <TextBlock Grid.Row="4" Text="Name" />
                <TextBox Grid.Row="6" Text="{Binding SelectedItem.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="8" Text="Email" />
                <TextBox Grid.Row="10" Text="{Binding SelectedItem.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="12" Text="Phone" />
                <TextBox Grid.Row="14" Text="{Binding SelectedItem.Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="16" Text="Address" />
                <TextBox Grid.Row="17" Margin="0,6,0,0" Height="24" VerticalAlignment="Top"
                         Text="{Binding SelectedItem.Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <StackPanel Grid.Row="17" VerticalAlignment="Bottom" Margin="0,0,0,0">
                    <TextBlock Text="Discount" Margin="0,12,0,6" />
                    <TextBox Text="{Binding SelectedItem.Discount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <Button x:Name="saveButton" Content="Salvesta" Margin="0,16,0,0" Height="30"
                            Command="{Binding SaveCommand}"
                            CommandParameter="{Binding SelectedItem}" />
                    <Button x:Name="addButton" Content="Lisa uus" Margin="0,10,0,0" Height="30"
                            Command="{Binding AddNewCommand}" />
                    <Button x:Name="deleteButton" Content="Kustuta valitud" Margin="0,10,0,0" Height="30"
                            Command="{Binding DeleteCommand}"
                            CommandParameter="{Binding SelectedItem}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
